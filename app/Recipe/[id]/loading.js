import LoadingSpinner from "../../components/loadingSpinner"

/**
 * Loading state component for the Recipe page.
 *
 * @returns {JSX.Element} A loading spinner.
 */
export default function Loading() {

    return <LoadingSpinner />
}